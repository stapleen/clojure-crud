(ns backend.config)

(def db-config {:dbtype "postgresql"
                :dbname "name"
                :host "db"
                :user "user"
                :password "password"})

(def port 3000)