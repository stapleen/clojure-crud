(ns backend.config)

(def db-config {:dbtype "type"
                :dbname "name"
                :host "host"
                :user "user"
                :password "password"})

(def port 3000)